import React from 'react';
import Layout from '../components/Layout';

const Courses = () => {
    return (
        <Layout>
            <h1 className="text-2xl">Courses Page</h1>
        </Layout>
    );
};

export default Courses;
